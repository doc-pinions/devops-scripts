name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a simple command
        run: echo "Running tests or build steps..."

      - name: Another simple command
        run: echo "This is the devops-scripts project!"